# extraction-db

Place **PostgreSQL dump files** produced for PA2 submission here (e.g. `pg_dump` output).

Markers use this together with [`../README.md`](../README.md) restore instructions.

Large binary dumps are typically **gitignored**; commit only what the course asks for or host artefacts outside the repo if they exceed sensible size limits.
