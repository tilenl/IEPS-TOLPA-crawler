2. Redefine that the crawler should stop when it gets 5000 HTML pages in the database, and not 5000 rows in the pages table. This will enable us to ignore having ERROR, DUPLICATE, BINARY pages in the frontier. How will this effect the logic of throwing away Frontier pages, when the database storage is at full capacity? Also update the scripts/db-crawl-status.sh to track how many HTML pages till we get to 5000, and also how many pages/rows are in total.
5. how can we further optimize the crawler to really allow only the markdown and readme pages of real repositories to be added to the frontier? How will this affect when running the crawler to allow /topics pages, for getting more URLs to put into startingseededurls? For example https://github.com/ZhengPeng7 and https://github.com/rohitmenon86 is not a repository, but this is: https://github.com/catalyst-team/catalyst. As I have noticed, only one / path after the github.com is not a repository. Is it a good idea to remove these? We could loose on much of links that would enable us to get to the 5000 sites, but we could just add more URLs to the frontier to help us get to that mark. Or we could add additional marker for such sites, to not be marked as HTML, but as another type, which tells us we are only keeping it for getting to new URLs. Should we rename the /topic pages to TOPIC after we fetch them instead HTML, and add a application.properties for maxTopicPages, so we don't converge into them? This way we still add topic pages, to broaden our URLs in the frontiers, but do not count them as final HTML pages.